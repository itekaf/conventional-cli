/**
 * Possible type of argument
 * @type {enum}
 */
export enum ArgumentTypes {
    enum = 'enum',
    array = 'array',
    string = 'string',
    number = 'number',
    object = 'object',
    boolean = 'boolean',
    undefined = 'undefined',
}
